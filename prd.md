# Prompt Analyzer & Optimizer — PRD

**Версія:** 0.4  
**Попередня:** 0.3 (зафіксовано рішення замовника: MVP, дані, ключ, мови, репозиторій)

---

## 1. Огляд

**Prompt Analyzer & Optimizer** — внутрішній інструмент (з можливістю приватного онлайн-доступу), який допомагає швидко створювати якісні промпти, ітеративно аналізувати їх і отримувати рекомендації для покращення.

**Цінність:**

- Прискорити написання промптів.
- Підвищити їх якість через структурований зворотний зв’язок.
- Навчати користувача через пояснення (explainability).

---

## 2. Цілі та метрики успіху

### 2.1 Основні цілі

- Структурований аналіз промптів (оцінка + розбір за критеріями).
- Автоматичне покращення тексту промпта з поясненням змін.
- Підтримка ітеративного циклу «аналіз → покращення → повтор».

### 2.2 Метрики успіху (MVP)

- Час від введення промпта до першого корисного результату.
- Зміна оцінки після дії «Improve» (до/після).
- Кількість ітерацій на сесію / користувача.
- Внутрішня оцінка задоволеності (опитування / якісний фідбек).

---

## 3. Цільова аудиторія

| Сегмент | Опис |
|--------|------|
| Розробники | Junior → Senior |
| Нетехнічні користувачі | Потребують простого режиму |
| Power users | Розширений аналіз, деталі |

**Рівні складності контенту:** початковий / середній / просунутий (впливає на глибину пояснень у Advanced mode).

---

## 4. Функціональні вимоги

### 4.1 Аналіз промпта (FR-1)

- Загальна оцінка в діапазоні **1–10**.
- Розбір за критеріями (окремі нормалізовані бали):
  - Clarity  
  - Context  
  - Specificity  
  - Constraints  
  - Output Format  

**Прийнятність:** усі критерії присутні в UI; значення узгоджені з відповіддю API (structured output).

### 4.2 Рекомендації (FR-2)

- Список дій: що **додати**, що **прибрати**, що **уточнити**.

### 4.3 Покращення промпта (FR-3)

- Згенерований переписаний промпт.
- Покращена структура та оптимізація під LLM.
- **Explain changes:** що змінено і чому (обов’язково в Advanced; у Simple — скорочено або приховано згідно з режимом).

### 4.4 Режими (FR-4)

| Режим | Поведінка |
|-------|-----------|
| **Simple** | Мінімум деталей, швидкий результат |
| **Advanced** | Повний аналіз, пояснення змін, деталізація |

### 4.5 Історія та версії (FR-5)

- Збереження промптів і результатів аналізу/покращення.
- Ланцюжок версій: original → improved → наступні ітерації (v1, v2, v3…).
- Відстеження ітерацій.
- **Diff між версіями** (візуальне порівняння тексту).
- **Персистентність (MVP):** дані зберігаються **локально** (наприклад IndexedDB / localStorage з обмеженнями); закладається можливість **подальшої міграції** на сервер після з’явлення backend та auth.

### 4.6 Шаблони (FR-6)

- Базовий набір категорій: **coding**, **marketing**, **fitness**, **general tasks**.
- Шаблон підставляє стартовий текст або структуру в редактор.

### 4.7 Тестування промпта (FR-7, поза MVP)

- Кнопка на кшталт «Show AI response» / симуляція відповіді моделі для швидкої перевірки.
- Не входить у перший реліз MVP; закладається в архітектуру за можливості.

---

## 5. Користувацький сценарій (основний цикл)

1. Користувач вводить або обирає шаблон і редагує промпт.
2. Запускається аналіз.
3. Відображаються: загальна оцінка, breakdown, рекомендації.
4. Користувач натискає «Improve».
5. Показується новий промпт і пояснення змін (залежно від режиму).
6. Збереження в історію; можливість повторити цикл або порівняти версії.

---

## 6. Обробка AI (multi-step pipeline)

Послідовність кроків (кожен крок — **окремий** запит до моделі, не один «все в одному»):

1. **Analysis** — оцінка та breakdown.  
2. **Suggestions** — рекомендації.  
3. **Improvement** — переписаний промпт.  
4. **Explain** (переважно Advanced) — пояснення змін.

**Принцип:** структуровані відповіді (наприклад JSON / structured outputs), ролі на кшталт «prompt engineering expert», мінімізація галюцинацій через жорсткий формат.

**Опціонально для продуктивності:** паралельні виклики там, де немає залежності даних (наприклад analysis + suggestions — лише якщо контракт API це дозволяє без втрати якості).

---

## 7. Технологічний стек (цільовий)

| Шар | Технології |
|-----|------------|
| Frontend | Angular, HTTP-клієнт (наприклад Axios або `HttpClient`), стан: RxJS / Signals |
| AI | OpenAI API (на кшталт GPT-4o-mini), structured outputs |
| Backend | Пізніше: Node.js (auth, rate limit, proxy до AI), приватний API-шар |

**Репозиторій і UI-фундамент:** окремий репозиторій / окремий додаток (не monorepo з ecolabel-apps). Токени теми, світла/темна тема, кольори та шрифти **копіюються** з проєкту `ecolabel-apps/frontend` (узгоджений вигляд без спільного npm-пакета на етапі MVP).

---

## 8. Автентифікація, доступ і секрети

**Внутрішній MVP**

- Ключ OpenAI задається через **змінні середовища на клієнті** (наприклад `.env` у збірці Angular) — лише для **внутрішнього** користування; не для публічного продакшену.

**Після MVP / продакшен**

- Приватний доступ; проста auth (JWT / OAuth).
- API-ключі лише на **сервері** (proxy); клієнт не містить секретів.

---

## 9. Локалізація та мова відповідей AI

**Мови інтерфейсу:** English, Ukrainian, Polish.

**Мова відповідей моделі (аналіз, рекомендації, improve, explain):**

- За замовчуванням — **та сама мова, що обрана для UI**.
- Окремий елемент керування: **«Мова відповіді AI»** (може збігатися з UI або відрізнятися — наприклад UI українською, пояснення англійською).

Запити до API повинні явно враховувати обрану «мову відповіді AI» (system / user instructions або параметр, узгоджений з імплементацією).

---

## 10. UX-напрям

- Інтерфейс «інструменту для роботи»: мало зайвих кроків.
- Швидкий доступ до: Analyze, Improve, History, (у майбутньому) Test prompt.

---

## 11. Межі релізу MVP

### 11.1 Обов’язково в MVP (зафіксовано)

- FR-1 Аналіз (оцінка + критерії).  
- FR-2 Рекомендації.  
- FR-3 Покращення + explain (хоча б у Advanced; у Simple — згідно з FR-4).  
- FR-4 Simple / Advanced.  
- FR-5 **Повна історія:** збереження, версії, перегляд ланцюжка, **diff між версіями**; стартово **локальне** сховище з проєктом міграції на сервер.  
- FR-6 **Шаблони** (coding, marketing, fitness, general tasks).  
- Локалізація UI + **перемикач «мова відповіді AI»** (розділ 9).  
- Внутрішній збірковий ключ через `.env` на клієнті (розділ 8).

### 11.2 Поза MVP (наступні ітерації)

- FR-7 Тестування промпта (симуляція відповіді).  
- Серверна персистентність історії після auth / backend.  
- Розширена аналітика (тренди оцінок).  
- Learning mode (підказки, навчальні блоки).  
- Інтеграції (IDE, розширення браузера).

---

## 12. Нефункціональні вимоги та ризики

**Продуктивність і вартість**

- Латентність через багатокроковий pipeline; кешування результатів аналізу де доречно.
- Масштабування вартості API.

**Якість**

- Узгодженість оцінок між запусками (варіативність моделі — очікуваний ризик).

**UX**

- Баланс між «power tool» і простотою Simple mode.

---

## 13. Відмінність від «просто rewriter»

Продукт акцентує:

- Структурований аналіз і критерії.  
- Пояснюваність змін.  
- Ітеративний цикл і версійність.

---

## 14. Прийняті рішення (замовник)

| Тема | Рішення |
|------|---------|
| Склад MVP | Обов’язково: **шаблони (FR-6)**, **diff версій**, **повна історія (FR-5)** |
| Дані | Спочатку **локально**; **міграція на сервер** пізніше |
| Ключ API (MVP) | Тимчасово **`.env` на клієнті** для внутрішнього використання |
| Мови AI | За замовчуванням **як мова UI**; окремо — **перемикач «мова відповіді AI»** |
| Репозиторій / тема | **Окремий** репозиторій/додаток; **копія** токенів і теми з `ecolabel-apps/frontend` |

### Відкриті питання (опційно)

- **Оцінка 1–10:** чи потрібна калібровка / референсні приклади в системних промптах для стабільності між сесіями?

---

## Додаток: зміни від v0.2

- Узгоджено перелік MVP з розділами про історію та шаблони; додано таблиці та коди вимог FR-*.
- Явно відокремлено FR-7 (тест промпта) як post-MVP.
- Додано нефункціональні аспекти та відкриті питання.
- Прибрано надлишкові декоративні елементи з заголовків для кращої читабельності в репозиторії.

## Додаток: зміни в v0.4

- Зафіксовано рішення замовника в розділі 14 та в межах MVP (§11.1).
- Додано: локальне сховище + майбутня міграція (FR-5), `.env` на клієнті для внутрішнього MVP (§8), мова відповіді AI (§9), окремий репозиторій і копія теми з ecolabel-apps (§7).
